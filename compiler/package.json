{
  "name": "easys",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "bin": {
    "easys": "dist/cli/easys.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "prepare": "npm run build",
    "cli": "node dist/cli/easys.js"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.3",
    "vitest": "^2.1.8"
  }
}
